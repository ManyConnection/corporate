[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
  force = false
  # POST のときだけ書き換える
  conditions = {RequestMethod = ["POST"]}
